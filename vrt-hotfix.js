/*
  vrt-hotfix.js — NO LONGER IN USE (v10+)

  This file is kept as a harmless stub so that any browsers still
  serving the old cached index.html (v8) do not get a 404 error
  when they try to load this script.

  The new index.html (v10 stable) does NOT load this file.
  All scanning logic is now self-contained in index.html.
*/
